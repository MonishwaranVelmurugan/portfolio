import TechnologyBadge from '../TechnologyBadge'

/** @deprecated Use TechnologyBadge directly. Kept for backward compatibility. */
export default function TechnologyTag(props) {
  return <TechnologyBadge {...props} />
}
